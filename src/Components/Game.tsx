import { Container } from '@mui/material';
import TitleWithGradient from './Styled/TitleWithGradient';

const Game = () => {
  return (
    <Container maxWidth={'sm'}>
      <TitleWithGradient>Black friday sale</TitleWithGradient>
    </Container>
  );
};

export default Game;
