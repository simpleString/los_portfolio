import styled from '@emotion/styled';
import Typography from '@mui/material/Typography';
import { TypographyProps } from '@mui/material';

const CardText = styled(Typography)<TypographyProps<any>>`
  font-weight: 600;
  font-size: 16px;
`;
export default CardText;
